export interface commentInteracitonDB {
    comment_id: string,
    user_id: string,
    interaction: boolean
}

export interface Comment {
    commentId: string;
    userId: string;
    interaction: boolean
}
